# Datasets

This folder is reserved for curated sample datasets, benchmark inputs, and future reference tables.

The current application uses deterministic emission factors in code, so no large dataset is required for the core workflow.
